// const userModel = require('../models/user.model') // Thay đổi đường dẫn đến mô hình của bạn

// const migrate = async () => {
//   try {
//     // Thêm trường mới vào mô hình
//     await userModel.updateMany({}, { $set: { is2FAEnabled: false } })

//     console.log('Migrate thành công!')
//   } catch (error) {
//     console.error('Lỗi khi migrate:', error)
//   }
// }

// // Gọi hàm migrate
// migrate()
